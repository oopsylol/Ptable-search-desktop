/**
 * 简化设置页面组件
 * 在主窗口中不显示，因为现在使用独立的设置窗口
 */
import React from 'react';

/**
 * 简化设置页面组件
 * 现在只返回null，因为设置页面在独立窗口中显示
 */
const SimpleSettingsPage: React.FC = () => {
  // 在主窗口中不显示设置页面，因为现在使用独立窗口
  return null;
};

export default SimpleSettingsPage;